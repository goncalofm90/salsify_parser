# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# gem 'ftp for ftp connections'
gem 'net-ftp'

# gem 'ox' for parsing because for this exercise we are doing xml only so it's way faster than nokogiri
gem 'ox'

# gem 'faraday' for http connections
gem 'faraday'

# gem 'dotenv' for loading env variables from .env file
gem 'dotenv', groups: [:development, :test]

# logging gem
gem 'logger'
